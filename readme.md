## VSTS (Azure Pipelines) agent docker image

Based off `Ubuntu 16.04` and bundled with `python2`, `kubectl` and `docker cli`.
Can be run in Kubernetes as ephemeral agent, automatically (de-)registers itself.
